<!DOCTYPE html>
<h1 style="text-align: center; margin-bottom: 20px;">Party Ideas for <%= name %> (Age: <%= age %>)</h1>

<form action="/save" method="POST" style="display: flex; flex-direction: column; gap: 10px; max-width: 600px; margin: 20px auto; padding: 20px; border: 1px solid #ccc; border-radius: 5px;">

  <table style="width: 100%; border-collapse: collapse;">
    <thead>
      <tr>
        <th style="padding: 8px; border: 1px solid #ccc; background-color: #f2f2f2;">Idea</th>
        <th style="padding: 8px; border: 1px solid #ccc; background-color: #f2f2f2;">Save</th>
      </tr>
    </thead>
    <tbody>
      <% ideas.forEach(idea => { %>
      <tr>
        <td style="padding: 8px; border: 1px solid #ccc; white-space: pre-wrap; font-family: monospace;"><%= idea %></td>
        <td style="padding: 8px; border: 1px solid #ccc; text-align: center;">
          <input type="checkbox" name="ideas" value="<%= idea %>">
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>

  <button type="submit" style="padding: 10px 15px; background-color: #4CAF50; color: white; border: none; border-radius: 3px; cursor: pointer;">Save Ideas</button>
</form>